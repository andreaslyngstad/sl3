<!DOCTYPE html>
  <head>
    <title>Squadlink</title>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <script type="text/javascript">
      var lang = "<%= current_firm.language%>"
    </script>
      
      <%= stylesheet_link_tag('invoice', :media => "all") %>
      <%= stylesheet_link_tag('normalize', :media => "all") %>
      <%= javascript_include_tag('invoice_pdf', :media => "all") %>
       <script type="text/javascript">
     $(document).ready(function() {
      $('.invoice_wrapper').find('.page_counting').count_pages()
      $('.invoice_table').put_total_at_bottom();
    })
    </script>
    <link rel="shortcut icon" href="/favicon.png" />

    <%= yield :head %>
  </head>
  <body> 
  	<span class="current_firm_data" data-clockFormat="<%= current_firm.clock_format%>" data-timeFormat="<%= current_firm.time_format %>" data-dateFormat="<%= current_firm.date_format %>" data-currency="<%= current_firm.currency%>" data-language="<%= current_firm.language%>"></span>
		<div id="main_container">
		  
      <%= yield %>
		</div>	
  </body>
</html>